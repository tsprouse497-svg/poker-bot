# Roadmap

## V1

The v1 sequence is locked by `phase_status.yml` and the contracts under
`docs/phase_contracts/`.
All ten phases are complete, so the sequence is finished rather than paused.

## V2

Nothing is declared after v1.
`docs/V2_ROADMAP.md` proposes a seven-phase v2 sequence and lists the questions
that block it; `phase_status.yml` knows about none of those phases and will not
until a separate task adopts the proposal.

## Deferred

- PokerNow automation.
- Browser/platform observation.
- Training UI package.
- Runtime solver calls.
- Large corpus hand-history ingestion.
