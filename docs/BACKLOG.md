# Backlog

| ID | Status | Phase | Title | Reason |
| --- | --- | --- | --- | --- |
| V2-POKERNOW-AUTOMATION | deferred | v2 | PokerNow automation and browser observation | Explicit v1 non-goal. |
| V2-TRAINING-UI | deferred | v2 | Training UI package | V1 is CLI and report driven. |
| V2-LARGE-DATASET-COMPARISON | deferred | v2 | Large hand-history corpus comparison | V1 allows only tiny normalized sample comparison. |
| CONTRACT-REPORT-PATHS | done | contract-update | Point Phase 01/07/08 contract report paths at distinct report files | Resolved by MAINT-01-AGENT-STRUCTURE; every contract now names a distinct report file and the shared latest_replay_report.txt path is gone. |
| UNDER-RAISE-ACCUMULATION | deferred | engine | Reopen betting when consecutive short all-ins reach a full raise | V1 keeps seats barred once an under-raise all-in occurs even if later short all-ins cumulatively amount to a full raise; real NLHE reopens raising there. Fail-closed but stricter than the live rule. |
| SECOND-ORBIT-PREFLOP-SPOTS | deferred | charts | Chart spots needing a position to act twice | V1 spot keys allow one entry per position, so any spot where a position acts twice (hero facing a 4bet or later) has no representable key; lookup misses those fail closed instead of guessing. |
| STACK-DEPTH-BUCKETS | deferred | charts | Stack-depth bucketing for chart lookup | V1 lookup requires an exact stack_depth_bb match. Nearest-depth or bucketed matching would be a heuristic, so an uncovered depth fails closed. |
| ASYMMETRIC-EFFECTIVE-STACKS | deferred | charts | Per-seat effective stack depth in chart spots | stack_depth_bb is one table-wide number, so hero at 100bb facing a 35bb opener cannot be expressed or queried; a caller must pick one number and silently answers a different question. |
| BLIND-STRUCTURE-VARIANTS | deferred | charts | Antes and straddles in the artifact format | The artifact declares no blind structure, so a straddled or anted pot reads as an ordinary one even though both materially change correct preflop ranges. Common in the home-game context this bot targets. |
| CHART-COVERAGE-EXPANSION | deferred | charts | More chart depths and spot classes beyond six-max 100bb first-two-decisions | Phase 05 commits one six-max 100bb solution covering opens, spots facing a single open, the opener facing a three-bet, and the big blind facing a limp. Other depths and the squeeze and cold four-bet classes are already representable and already indexed by depth, so they are additive data rather than format work; every uncovered spot refuses meanwhile. |
| RAISE-SIZE-IN-SPOT-KEY | deferred | charts | Record raise sizes in preflop spot keys | V1 spot keys record action names without sizes, so a 2.5bb open and a 4bb open share one spot. Sizing-aware spots need a size vocabulary the artifact format does not yet declare. |
| FOLD-WHEN-FREE | deferred | engine | Accept recorded folds when checking is free | The engine treats fold as illegal when to_call is zero, so real histories containing open folds (timeouts, surrendered rivers) fail replay; the strategy contract inherits the same invariant. |

Generated by `scripts/generate_backlog.py`.
