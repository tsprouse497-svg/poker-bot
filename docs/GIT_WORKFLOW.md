# Git Workflow

- Local commits are source of truth until a GitHub remote exists.
- After a remote exists, use branch and PR workflow.
- Verify before commit and before push.
- Phase gates need committed audit packets.
- Do not rewrite history unless explicitly part of a maintenance task.
- Phase status advances only after the phase packet passes.
