auto_advance: false
halt_reason: >-
  Halted 2026-08-30. Live ruling is decision 19 (2026-08-31): re-solve with realization=static.
  Decisions 17 and 18 are superseded and 18 is withdrawn - ignore both. No work is owed in GTOpen and
  no fork exists; ~/projects/gtopen is a reference clone at 4aee435 and stays unmodified.
  What stopped the phase: with realization=calibrated the chart folds JJ/TT/99/88 at four-bet nodes
  while calling 76s/87s, because calibrated prices flops from 169 per-class numbers fitted only on
  single-raised and three-bet pots and applies them unchanged at SPR 1.67. A one-field experiment
  (calibrated vs static, same tree, both arms 400 iterations) showed static removes it. GTOpen ships
  calibrated, static and raw, and the field selecting one is RULED_CONFIG in this repo, in this
  phase's approved scope - so this is a config ruling, not a solver repair.
  To restart: flip realization to static in contract-update mode on decision 14's precedent, re-run
  extract_gtopen_preflop.py against an unmodified GTOpen at 4aee435, re-derive, then re-measure round
  2's two open blockers rather than assume they moved. Note add_allin=false and static have never been
  solved together; the experiment held add_allin=true to isolate one variable, so its numbers do not
  describe the build this produces. SOLVE_TARGET_GAP_BB stays 0.00016.
  The gate is red by design until the export is rebuilt - RULED_CONFIG disagrees with the committed
  export - and reverting the constants to green it would erase the rulings.
loop: halted
phase_id: '14'
schema_version: 1
stage: 1
stage_base: a386c77d1526dc1c17de97044373410a26a806e7
