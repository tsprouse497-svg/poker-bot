# MAINT-38 independent review: phase 16 sign-off record

Read-only review of `git diff 2d90379 d07fb70` in the `maint-38` worktree. I wrote none of phase 16,
none of its review notes, and none of this task's commit or its blocker audit. I did not see the
session in which Taylor spoke, so everything below about what he said rests on the ExecPlan's "The
sign-off, as given" section and cannot be checked against his own words. Run here: `check_scope.py`,
`check_file_sizes.py`, `quality_checks.py`, `generate_backlog.py --check` (all exit 0), a
re-implementation of `loop_stage.unresolved_blockers` over the 22 phase 16 notes (0 open bullets
now), and read-only `python -c` reads of the committed sample and index. This file is the only file
written.

## Blocker

- [resolved] The figure the ExecPlan says Taylor was given before he signed, "at this coverage the bot would still bet about one flop in 550", is wrong for the case he raised, and it runs about ten times in the bot's favour. The 40 of 22,100 is all three boards added together (`Kh7d2c` 24, `8c8d3c` 12, `9c8c7c` 4). But `PostflopBettingStrategy._resolve` (`src/poker_training_bot/strategy/postflop_betting.py:239-274`) looks a cell up by its exact spot key, and the key includes hero's seat. `index.json` lists five entries: four have hero in the big blind, and only `f/b:9c8c7c/t6/d100/BTN/BTN:raise@2.5,BB:call/f:BB:check/...` has hero on the button. So a button facing a human big blind has a cell on 4 of 22,100 flops, about 1 in 5,525. Even then it needs a covered preflop line (six-handed, 100bb, an open between 2.0 and 3.0bb, folded round to a big blind who calls) and a big blind who checks. The cell does bet 99.88% of the time (mean over its 152 class rows), so "bet" is fair once the cell is reached. "One flop" is also a flop on the covered line only, which the packet (`PHASE_16_POSTFLOP_BETTING.md:59-60`) says was 16.88% of flops dealt. The number is only in the ExecPlan, not in the packet or the backlog, and the sign-off's stated ground ("we've extracted and stored output") does not rest on it. Still, it is the written record of what he was shown before he agreed. Fix: correct the ExecPlan sentence, and tell Taylor the corrected figure before `--advance`. If he stands by the sign-off, record that. Nothing else needs to change.

  [resolved] 2026-09-24 by the coordinator: the ExecPlan sentence is corrected, Taylor was given 4 of 22,100 flops, about
  1 in 5,525, only on the covered line and only after the big blind checks, with the next decision still refusing, and he
  answered "yes, i still sign off." Recorded in the ExecPlan's correction section; the packet's sign-off never carried the figure.

## Non-blocker

- The packet's sign-off (`PHASE_16_POSTFLOP_BETTING.md:494-496`) states no more than the ExecPlan says Taylor ruled, and it matches decision 25 (`PHASE_16_POSTFLOP_BETTING_DECISIONS.md:2424` onward: "closes on machinery rather than on coverage ... a later phase funds the campaign"). It does leave out the main thing he was shown: in self-play the chart makes no flop bet and voids hands. The packet carries that elsewhere (:57-81), so this is completeness, not overreach. The packet is 497 lines against its 500-line cap, which leaves three lines. Any later amendment to this packet will hit the cap.
- The ExecPlan's account has two small drifts from the record. (1) "voids 4,543 hands the old fallback played to showdown": decision 25 says 5,365 voided hands against 4,543 fallback showdowns, which are two different numbers. (2) "a three-quarter ceiling on multiway flops": per `THE-PHASE-CAN-ANSWER-AT-MOST-THREE-QUARTERS-OF-CORPUS-FLOPS`, the 74.9% ceiling is mostly not about multiway pots. Only the 7.7-point multiway share is structural. The limped/4-bet share (10.0) and the out-of-band 3-bet share (7.3) are a chart gap and a price choice, and more work could fix both. The packet line's "the three-quarter ceiling" inherits the looser framing. Neither drift changes what was ruled.
- Marking class-A evidence bullets `[resolved]` is honest in substance: I re-checked that each owning finding is fixed. But a reader of a single note can be misled. In `stage-04-tests-poker.md`, `stage-04-repair-verification.md` and `stage-06-build-poker.md`, the evidence lines under a paragraph blocker now say `[resolved]` while the blocker's own bold head (B1, B2 ...) carries no marker. So the support looks closed and the finding looks open, the reverse of the truth. The same notes still hold the audit's twelve unmarked paragraph blockers, all fixed. The appended line says "every bullet above that lacked a marker was audited ... and marked where the fix or ruling is shown". That is accurate for bullets, but it says nothing about the paragraph heads, and "fix" is loose for class A (the fix belongs to the owning finding, not the bullet) and for the one class-C item (closed by decision 21, a ruling). Suggest also putting `[resolved]` on each unmarked bold paragraph head (the parser ignores it, a reader sees it), and wording the line as "marked because its owning finding is fixed or ruled, per the audit".
- `stage-08-review-mechanical.md:153` now carries two `[resolved]` markers, the new one on the head and the original on :156. That is redundant but correct.
- The `A-SAMPLE-WHOSE-SITUATIONS-DO-NOT-CLOSE-VOIDS-THE-FLOP-NOT-THE-TURN` extension is true as far as it goes. The `9c8c7c` c-bet cell is reachable when a human big blind checks: the BTN line is covered, the board class is held, and the flop line `BB:check` is the key's own. In self-play it is not reachable, because no big-blind cell exists on that board class. But "part of the closure gap is an artefact of self-play" mislabels which gap is meant. Self-play explains the entry gap (the bot's other seat has to act first). The closure gap is the successors, and it is still real at a live table. After the bot's c-bet, a check-raise leads to an uncommitted node, and a call leads to the turn, so the bot's next decision refuses anyway unless the big blind folds. One added sentence would keep a reader from taking "overstates the harm" as "mostly fine at a table". The same logic helps the big-blind side too: facing a human button's 33% bet, the `Kh7d2c` facing-a-bet cell becomes reachable. The two self-play decisions never got there.
- The `REVIEW-QUEUE-COUNTS-EVIDENCE-BULLETS-AS-BLOCKERS` extension checks out: 12 A, 5 B, 1 C, and 12 paragraph blockers. I confirmed the continuation-line `[resolved]` shape at `stage-08-review-mechanical.md:156`, and that the parser (`scripts/loop_stage.py:168-185`) only tests lines starting with `- `, so a bold paragraph is never seen.
- The `scope_change_log` entry is accurate on paths and on "nothing else in it moves" (the packet diff is additions only). Its phrase "only the ones it shows fixed, with evidence, are marked resolved" is looser than what happened: twelve of the marked lines are evidence bullets, not fixes, and one is a ruling.
- Spot-checks of the audit against the current tree all held. `canonical_hole_cards` returns `min` over `board_suit_maps` (`postflop_isomorphism.py:139-165`). `servable` is counted only after the covered-line and fetched-board gates (`generate_postflop_betting_report.py:1436-1448`, guard at :311, called at :1740). The driver reads `arena_mb`, `exploit_pct` and `iteration` through `numeric` (`postflop_solve_driver.py:460`, :488-489), and `answered` raises on a missing field (`postflop_transport.py:68-78`). `seated` lists all six seats (`tests/test_postflop_betting.py:82-104`), and `git log -S` points to `e67104d`. `MENU_FRACTION_TOLERANCE = 0.05` (`postflop_key.py:100`). The reachability walk calls `postflop_action_order` (`postflop_key.py:356`). Every `f:none` key in `index.json` is hero BB. All six named commits exist with matching subjects. One small offset: decision 14's `frozen-into-data` line is at :1702, not :1704.
- Scope: `git diff --name-only` shows 14 paths, all under approved scope or standing scope. Nothing under `data/`, `src/`, `tests/`, `scripts/` or `docs/phase_contracts/` changed. `check_scope.py` passes, and `docs/BACKLOG.md` is current.

## Alignment

- A refusal at a real table does not void the hand the way it does in self-play: the hand goes on and the bot has to do something. The ExecPlan's framing, and the backlog extension's "overstates the harm at a real table", both assume that some behaviour follows a refusal, and nothing in this diff says what that behaviour is. This belongs to whichever phase first seats the bot at a live table (20 by the boundaries), not to this task. I did not check whether it is already filed.

## What I looked at outside the brief, and what I held back

Outside the brief: I read the strategy's lookup path and the index entries to test the button-only coverage, which is how the one-in-550 error turned up. I computed the c-bet cell's mean bet frequency. I compared the ExecPlan's account against decision 25's own figures, and the "three-quarter ceiling" wording against its backlog entry. I checked that the notes a reader would open now read the right way round, not just that the parser clears them.

Held back: I did not re-derive the audit's twelve paragraph-blocker verdicts beyond the spot-checks above. I did not re-run the 20,000-hand simulation or re-count the 1,755 board classes. I did not check whether the "what happens after a refusal at a live table" question is already in `backlog.yml`. I did not read `verification/loop_runs/16.yml`, because this diff does not touch it.
