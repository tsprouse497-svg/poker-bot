# MAINT-37 independent review

I am a read-only review subagent and wrote none of this task. I read `git diff f6dfe01 HEAD` in the
`maint-37` worktree (seven files), `AGENTS.md`, the ExecPlan, `docs/LOOP.md`, `lanes()`, `worktrees()`,
`pointers()`, `print_tick` and `print_integrate` in `scripts/loop_fleet.py`, `run_paths()`,
`state_path_for()` and the stage table in `scripts/loop_stage.py`, the lane consumers in
`scripts/review_queue.py`, the two pointer backlog entries, and the history of every pointer on `main`.
I ran `pytest tests/test_loop_fleet.py` (38 passed; 42 after `04503fa`), `ruff check` (clean), `check_scope.py`,
`quality_checks.py` and `freeze_tests.py --check` (all exit 0), `loop_fleet.py` and
`review_queue.py --list` from this worktree and from `main`, and the canary plus two other mutations
on a `git archive` copy in my scratch directory. I did not run `run_verify.py` or `check_gate_bite.py`,
and I changed no file except this note.

## Blocker

- [resolved] Fixed in `04503fa`: `lanes()` (`scripts/loop_fleet.py:221`, `:256-261`) now also reads the copy in the phase's own `phase/NN-` worktree and takes whichever is further along, `main` on a tie, so my scratch scenario returns the lane's stage-11 halt and `AGENTS.md:18-19` holds again. Original finding: the new rule makes `AGENTS.md` false for a phase that has merged but whose loop is still live, and `AGENTS.md` says it wins over any document that contradicts it. `AGENTS.md:18-19` tells a session that "a lane's live pointer is `verification/loop_runs/NN.yml` in the lane's own tree", while `lanes()` at `scripts/loop_fleet.py:232-240` now ignores the lane's own tree entirely once `main` holds any copy for that phase, and `docs/LOOP.md:33` says so. This is not only wording. Phase 16 is in exactly that state today (merged, `main` at stage 11 running), and a session that follows `AGENTS.md` and runs `loop_stage.py --phase 16 --halt` or `--advance` in the phase-16 worktree writes a state the board and the queue will no longer show. I reproduced it on a scratch fleet: `main` at stage 10 running and the lane at stage 11 halted with a sign-off reason gives `lanes() == [("16", main, 10, "running")]`, so the halt ask and the stage-11 policy ask both vanish from `review_queue.py` until the lane is merged again. The old code listed both copies, so the ask was visible (twice). The stage-10 instruction at `scripts/loop_stage.py:620-625` ("tag ..., reset to idle, gate again, commit, merge the branch") invites exactly this ordering, merging while the pointer still reads 10 and advancing to 11 in the lane afterwards; phases 13, 14 and 16 happened to move the pointer to 11 before merging (`7dbcf00`, `57b3ea0`, `eb28319`), which is why it has not bitten. The fix is small and either way works: amend `AGENTS.md` step 2 (a scope widening with a log entry) to say that once a phase's pointer is on `main`, `main`'s copy is the live one and every later loop move for that phase is made there, or keep `AGENTS.md` and let a lane worktree on that phase's own `phase/NN-*` branch outrank `main`'s copy when it is further along or halted. The first matches the Objective and how MAINT-36 closed phase 14; it just has to be written where the rules live.

## Non-blocker

- The rule silently switches off when no worktree has `main` checked out (`scripts/loop_fleet.py:232-234`: `integrated` stays empty). On a scratch fleet with the primary checkout on another branch, every stale phase 14 copy returned as a lane. That is the "no main worktree, old behaviour" fallback the brief describes, but nothing warns, and `docs/LOOP.md` does not mention it. Reading `main`'s pointers with `git show main:` / `git ls-tree main`, the way `show()` already reads `phase_status.yml` and `depends_on`, would remove the dependency on which branch a checkout happens to hold, and would also stop an uncommitted edit in the `main` worktree from counting as `main`'s state.
- The within-worktree change in `pointer_states()` is right. It now lets a completed per-lane pointer shadow a live legacy `loop_state.yml` for the same phase, where the old loop skipped the completed one and then listed the legacy one. `state_path_for()` at `scripts/loop_stage.py:667-673` falls back to the legacy file only when the per-lane file does not exist, so the driver already treats the per-lane file as authoritative, and the board now agrees with it. When I first reviewed it, no test pinned it: replacing `setdefault` with plain assignment (last layout wins) left all 38 tests green. `04503fa` adds `test_a_completed_pointer_speaks_over_the_legacy_file_beside_it`, which covers it.
- Tests: the diff to `tests/test_loop_fleet.py` is additions only, and `test_only_running_and_halted_pointers_are_lanes` is untouched. The four new tests exercise the rule through `lanes()` on real files. The canary's find string occurs exactly once in `scripts/loop_fleet.py`, and applying it on the scratch copy reddens all four new tests. Dropping only `and not on_main` reddens `test_a_live_pointer_on_main_is_listed_once_from_main`. The one extra failure on the scratch copy, `test_the_lock_lives_in_a_directory_that_exists`, fails on the unmutated copy too, because a `git archive` has no git directory; it is environmental. `verification/freeze.lock` changes only the `tests/test_loop_fleet.py` hash, its count 34 to 38, and the floor 1202 to 1206, which is the same plus four.
- Real board: `review_queue.py --list` from `main` (old code) prints 246 items, 54 distinct; from this worktree it prints 34, all distinct. The 34 are exactly the old distinct set minus the 20 phase 14 items, with nothing added and nothing else lost. Phase 16's sign-off line ("sign off before the fleet moves past phase 16") appears once, down from three, and `loop_fleet.py` lists three lanes (15, 16, 17) with no phase 14.
- Phase 16 is now reported with branch `main` and worktree the primary checkout, so `--tick` runs its stage driver on `main` and `--integrate 16` prints `merge --no-ff main` into `main`. The second was already true before this task (the `main` copy was inserted first and won `live[0]`), so it is not a regression, but the rule makes it the only answer for every merged phase.
- Re-review of `04503fa` (`git diff 7b93cd0 04503fa`, seven files). 42 tests pass, and ruff, `check_scope.py`, `quality_checks.py` and `freeze_tests.py --check` are clean. The freeze lock moves 38 to 42 tests and the floor 1206 to 1210. The board and queue on real data are unchanged: 34 items, identical to the first pass, with phase 16 listed once. On a scratch copy, each mutation below is caught by at least one test. Counting every sibling copy again (`if True:` in place of `if phase_id not in integrated:`) reddens 7 tests, letting any branch outrank `main` reddens the new advanced-lane test, and handing ties to the lane reddens the tie test.
- The canary's replacement no longer matches its description. `stale-pointer-copy-outvotes-main` swaps `if phase_id not in integrated:` for `if False:`. That does not make sibling copies count again. Instead it drops every unmerged lane, or raises a KeyError when an unmerged lane sits on its own `phase/NN-` branch, and that is why its reddened tests are `test_only_running_and_halted_pointers_are_lanes` and the unmerged-lane tests. It still bites, but it guards a different failure than the one it names. Replacing with `if True:` is the mutation the description describes, and I measured it reddening 7 tests.
- A resumed halt looks less far along than the halt it cleared. If a lane's halt is merged to `main` and the lane then runs `--resume`, the lane reads stage 11 running and `main` reads stage 11 halted. `LOOP_RANK` ranks halted above running, so `main` wins and the answered halt ask stays on the queue until the resume is merged. This errs toward showing an ask, which is the safe direction. Worth one sentence in `docs/LOOP.md`, or ranking by stage only and letting the lane win any within-stage difference.
- A completed phase that is ever reopened in a fresh lane under the same phase id would be invisible until merged, because `main`'s completed copy retires it everywhere. Nothing does that today; noting it so a future re-cut does not assume the board will show it.
- `docs/LOOP.md:33-34` is accurate. Minor precision: stale copies arrive in any worktree that merged or rebased onto `main` after the lane merged, not only ones that branched then. No em dashes in any changed file. The diff touches only approved paths plus `CURRENT_TASK.yml` and the ExecPlan, and `check_scope.py` agrees.

## Alignment

- `COMPLETED-LANE-POINTERS-ARE-NEVER-RETIRED` says "loop_fleet.lanes() filters to running and halted, so the board is unaffected, which is most of why this went unnoticed". That was already wrong before this task (stale `running` copies in nine siblings were on the board), and this task fixes the board side of it only. The entry should get a two-line note saying the board no longer counts sibling copies of a pointer `main` holds, and that the root defect, pointers never retired and copied into every branch, plus `run_paths()` counting them, is still open. Done in `04503fa` (`backlog.yml:2669-2673`), and the wording is accurate: I counted the nine sibling copies reading `running`. `LOOP-LANE-POINTERS-NEVER-RETIRE` is not contradicted, since it is about the stage driver, and needs nothing.
- `FLEET-AND-STAGE-DRIVERS-DISAGREE-ABOUT-INTEGRATION` already covers the stage-10 merge ordering. This task makes that ordering matter more, because a merge made before the stage-11 advance now hides the lane's newer pointer, not just duplicates it. Done in `04503fa` (`backlog.yml:2582-2584`), and with the fix it now correctly says the order no longer changes what the board shows, leaving only which order is intended unruled.

## What was not checked

- The full gate and `check_gate_bite`, by instruction. So I have not seen the canary judged by the real mutation runner, only by pytest on a copy.
- The coordinator's audit packet, which does not exist yet.
- `review_queue.py` from inside a lane worktree other than this one, and `--plan` output beyond the default board.
- Whether any sibling worktree holds uncommitted pointer edits; I read only the files on disk.
